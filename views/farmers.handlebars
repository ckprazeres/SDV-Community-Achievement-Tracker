Here is a list of your farmers. You can change the current farmer to track a different save file, add a new farmer, or delete a farmer.<br><br>

<section class="farmers-list">
{{#each farmers}}
	<div class="col-xs-12 col-sm-6 col-md-2">
		<div class="farmers-list-item">

			<form action="/user/farmers/delete/{{this.farmer_id}}" method="post">
				<button type="submit">x</button>
			</form>

			<form action="/user/farmers/update/{{this.farmer_id}}" method="post">
				<button type="submit">{{this.name}}</button>
			</form>
		</div>
	</div>
{{/each}}
</section>

<h2>Add A Farmer</h2>

<form method="post" action="/user/farmers/add">
  <div>
    <label>Farmer's Name</label>
    <input type="text" name="name" placeholder="i.e. Sally">
  </div>
  <button type="submit" class="btn btn-default">Add New Farmer!</button>
</form>